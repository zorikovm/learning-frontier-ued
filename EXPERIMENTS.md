# Журнал экспериментов

Во всех сравнениях использовался один и тот же агент PPO + LSTM и одинаковое число шагов среды. Восемь итоговых уровней применялись только для оценки, но не для выбора метода. Основным сигналом в коротких запусках была отдельная фиксированная выборка из 64 случайных лабиринтов.

## 1. Проверка исходного кода

**Цель.** Точно установить, как в JaxUED устроены PLR и ACCEL.

**Что проверено.** Буфер содержит 4000 уровней. Повторный выбор начинается после заполнения половины буфера. Затем его вероятность равна 0.8. При выборе учитываются ранг оценки с весом 0.7 и давность с весом 0.3. Температура ранга равна 0.3. PPO обновляется на повторно выбранных уровнях, но не на новых и измененных при `exploratory_grad_updates=false`.

Новые уровни — случайные лабиринты 13x13 с 25 стенами. ACCEL после повторного выбора применяет до пяти операций из `NO_OP`, `FLIP_WALL`, `MOVE_GOAL`.

**Вывод.** Изменения можно ограничить расчетом оценки, историей уровней и мутациями. `ActorCritic`, GAE и PPO update менять не нужно.

## 2. DR, 250 обновлений

**Гипотеза.** Короткий запуск должен подтвердить, что исходное обучение и оценка работают.

**Настройки.** Запуск 0, 250 обновлений, 2048000 шагов среды, исходные параметры PPO.

**Результат.** Доля решенных итоговых уровней 0.0000. Время 1189.8 с.

**Вывод.** Запуск корректен, но такой бюджет слишком мал для сравнения методов подбора уровней.

## 3. PLR MaxMC и ACCEL MaxMC, 250 обновлений

**Гипотеза.** Нужно получить исходные точки сравнения и проверить повторный выбор с мутациями.

**Настройки.** Запуск 0, по 250 обновлений на метод. У PLR мутации выключены, у ACCEL выполняется пять операций.

| Метод | Доля решенных итоговых уровней | Время |
|---|---:|---:|
| PLR MaxMC | 0.0000 | 845.0 с |
| ACCEL MaxMC | 0.0125 | 672.2 с |

У ACCEL единственный успех из десяти получен на SixteenRooms. Это слишком мало для содержательного преимущества.

**Вывод.** Оба исходных метода работают. По скорости полное обучение на процессоре занимает десятки часов, поэтому запуски на 30000 обновлений локально не проводились.

## 4. Проверка новых оценок

**Гипотеза.** MNA и история успеха должны компилироваться внутри исходного JAX-кода, не меняя PPO.

**Реализация.** Добавлены MNA, сглаженная частота успеха `p`, оценка `4p(1-p)` и положительное изменение `p`. Для каждого уровня хранятся число посещений, успехи, завершенные эпизоды и максимальная награда.

**Настройки.** Запуск 0, 1–2 обновления для PLR и ACCEL.

**Результат.** Все варианты завершились, записали конечные оценки и состояние буфера. Unit-тест отдельно проверяет, что MNA имеет правильную форму и равен нулю для ни разу не решенного уровня.

**Вывод.** Реализация работает. На первом посещении `4p(1-p)` симметричен для успеха и неудачи, поэтому его нужно оценивать после нескольких повторных прохождений.

## 5. MaxMC, MNA и история успеха, 250 обновлений

**Гипотеза.** MNA и `4p(1-p)` должны слабее зависеть от текущего успеха, чем MaxMC.

**Настройки.** PLR, запуск 0, 250 обновлений, одинаковая фиксированная проверочная выборка.

| Оценка | Проверочная выборка | Связь с текущим успехом | Успех среди верхних 10% |
|---|---:|---:|---:|
| MaxMC | 0.2563 | 0.537 | 0.351 |
| MNA | 0.2656 | 0.300 | 0.252 |
| `4p(1-p)` | 0.2125 | **0.190** | 0.452 |
| `4p(1-p)` + изменение `p` | **0.3250** | 0.458 | 0.998 |

**Наблюдение.** Чистая граница успеха действительно меньше связана с текущим успехом, но дает худший результат. Добавка изменения `p` повышает результат, однако почти всегда выбирает уже успешные траектории.

**Вывод.** MNA оставлен. Чистая граница успеха отвергнута как самостоятельная оценка. Изменение `p` нельзя считать надежной оценкой обучения.

## 6. PVL, ACCEL и первая комбинация

**Гипотеза.** Нужно проверить PVL, MNA с мутациями и произведение MNA на историческую оценку.

**Настройки.** Запуск 0, 250 обновлений на метод. Для анализа один раз сохранялась полная последовательность повторных посещений уровня.

| Метод | Проверочная выборка | Успех среди верхних 10% | Связь с последующим изменением после PPO |
|---|---:|---:|---:|
| PLR PVL | 0.2969 | 0.975 | -0.492 |
| ACCEL MNA | 0.2031 | 0.311 | -0.022 |
| ACCEL, фиксированные мутации | 0.1594 | 0.997 | -0.742 |
| ACCEL, адаптивные мутации | 0.2281 | 1.000 | -0.617 |
| `MNA * (граница + изменение)` | 0.2844 | 0.367 | -0.114 |

Адаптивный вариант использовал в среднем 2.22 изменения вместо 5 и улучшил ACCEL, но все равно проиграл PLR. PVL и изменение `p` отрицательно связаны с результатом следующего PPO update.

**Вывод.** PVL и отдельное изменение успеха отвергнуты. ACCEL на этом этапе слабее PLR. Произведение с MNA оставлено для дальнейшей проверки.

## 7. MNA умноженный только на `4p(1-p)`

**Гипотеза.** Удаление изменения `p` должно убрать смещение к случайно удачным траекториям.

**Настройки.** PLR, запуск 0, 250 обновлений.

**Результат.** Проверочная выборка 0.2375, итоговый набор 0.0000. Средняя частота успеха верхних уровней буфера 0.509.

**Вывод.** Teacher действительно выбирает промежуточную область, но слишком быстро понижает приоритет уже начавших решаться уровней. Чистое произведение отвергнуто.

## 8. MaxMC против MNA в трех запусках

**Гипотеза.** MNA должен хотя бы сохранить качество MaxMC и уменьшить зависимость от случайных ранних успехов.

**Настройки.** PLR, запуски 0, 1, 2, по 250 обновлений.

| Метод | Seed 0 | Seed 1 | Seed 2 | Среднее |
|---|---:|---:|---:|---:|
| MaxMC | 0.2563 | 0.2844 | 0.2156 | 0.2521 ± 0.0346 |
| MNA | 0.2656 | 0.2781 | 0.2406 | **0.2615 ± 0.0191** |

Средняя парная разница `+0.0094 ± 0.0156`. Результат на итоговом наборе равен нулю во всех запусках.

**Вывод.** Улучшение небольшое, но MNA заметно слабее связан с текущим успехом и имеет меньший разброс. Он оставлен для среднего запуска.

## 9. Небольшая добавка изменения успеха

**Гипотеза.** Коэффициент 1.0 слишком велик, а нулевой коэффициент слишком быстро исключает освоенные уровни. Один промежуточный коэффициент 0.25 может сохранить полезный уровень дольше.

**Оценка.** `MNA * (4p(1-p) + 0.25 max(p_new-p_old, 0))`.

**Настройки.** PLR, запуск 0, 250 обновлений.

**Результат.** Проверочная выборка 0.2906, итоговый набор 0.0000, успех среди верхних 10% оценок 0.335.

**Вывод.** Вариант прошел заранее выбранный фильтр: лучше MNA в том же запуске, но не смещается полностью к решенным уровням. Другие коэффициенты не перебирались.

## 10. Итоговый вариант в трех запусках

**Настройки.** PLR, запуски 0, 1, 2, по 250 обновлений.

| Метод | Seed 0 | Seed 1 | Seed 2 | Среднее |
|---|---:|---:|---:|---:|
| MaxMC | 0.2563 | 0.2844 | 0.2156 | 0.2521 ± 0.0346 |
| MNA | 0.2656 | 0.2781 | 0.2406 | 0.2615 ± 0.0191 |
| Итоговый метод | **0.2906** | **0.3219** | **0.2469** | **0.2865 ± 0.0377** |

Средняя парная разница с MaxMC равна `+0.0344 ± 0.0031`. Новый вариант обошел MaxMC и MNA в каждом запуске. Результат на итоговом наборе у всех равен нулю.

**Вывод.** Вариант выбран для более длинного запуска. Это выбор по отдельной случайной выборке, а не по итоговым уровням.

## 11. Сравнение на 1000 обновлений

**Гипотеза.** Нужно проверить, сохраняется ли преимущество после более долгого обучения.

**Настройки.** PLR MaxMC, PLR MNA и итоговый метод; запуск 0; по 1000 обновлений, или 8192000 шагов среды.

| Метод | Итоговый набор | Проверочная выборка | Средняя награда |
|---|---:|---:|---:|
| MaxMC | 0.0250 | 0.8469 | 0.6407 |
| MNA | **0.0375** | 0.8406 | 0.6573 |
| Итоговый метод | 0.0250 | **0.8531** | **0.6680** |

Ход проверки:

| Метод | 250 | 500 | 750 | 1000 |
|---|---:|---:|---:|---:|
| MaxMC | 0.2750 | **0.5781** | **0.8156** | 0.8469 |
| MNA | **0.2750** | 0.4719 | 0.7594 | 0.8406 |
| Итоговый метод | 0.2656 | 0.5063 | 0.8062 | **0.8531** |

MaxMC лидировал на 500 и 750 обновлениях, новый метод вышел вперед только к 1000. Значит, порядок методов на коротком бюджете может измениться.

На итоговых уровнях решается только SixteenRooms: 0.2 у MaxMC, 0.3 у MNA и 0.2 у нового метода. Один дополнительный успех MNA не дает основания считать его лучше.

**Вывод.** Итоговый метод оставлен главным кандидатом для полного сравнения. Результаты на 30000 обновлений приведены ниже.

## 12. Полный бюджет, запуски 0–2

**Гипотеза.** Преимущество короткого запуска должно сохраниться на полном бюджете.

**Настройки.** DR, PLR MaxMC, ACCEL MaxMC и итоговый метод; запуски 0, 1, 2; по 30000 обновлений, или 245760000 шагов среды. Агент и PPO одинаковы. Итоговые уровни используются только для оценки.

**Результат отдельной оценки контрольной точки.**

| Метод | Запуск 0 | Запуск 1 | Запуск 2 | Среднее ± отклонение |
|---|---:|---:|---:|---:|
| DR | 0.3000 | 0.4500 | 0.5750 | 0.4417 ± 0.1377 |
| PLR MaxMC | 0.2875 | 0.5875 | 0.2125 | 0.3625 ± 0.1984 |
| ACCEL MaxMC | 0.2000 | 0.8125 | 0.5875 | 0.5333 ± 0.3098 |
| Итоговый метод | **0.7125** | 0.5000 | 0.4125 | **0.5417 ± 0.1543** |

**Наблюдение.** Новый метод дал лучшее среднее и заметно меньший разброс, чем ACCEL, но результат ACCEL сильно зависит от запуска.

**Вывод.** Трех запусков мало для сравнения с ACCEL, поэтому для двух лучших методов добавлены запуски 3–5.

## 13. Дополнительные запуски для ACCEL и итогового метода

**Гипотеза.** Еще три запуска с одинаковыми начальными значениями генератора должны показать, сохраняется ли разница с ACCEL.

**Настройки.** ACCEL MaxMC и итоговый метод; запуски 3, 4, 5; 30000 обновлений.

| Метод | Запуск 3 | Запуск 4 | Запуск 5 | Все 6 запусков, среднее ± отклонение |
|---|---:|---:|---:|---:|
| ACCEL MaxMC | 0.6375 | 0.3625 | 0.4750 | 0.5125 ± 0.2158 |
| Итоговый метод | **0.7000** | **0.6625** | 0.3125 | **0.5500 ± 0.1670** |

Парные разности `наш метод - ACCEL`: `+0.5125, -0.3125, -0.1750, +0.0625, +0.3000, -0.1625`. Средняя разность `+0.0375`, 95-процентный интервал `[-0.2953, 0.3703]`.

**Наблюдение.** Новый метод выиграл в трех запусках из шести. Среднее выше на 0.0375, разброс ниже на 0.0488. Интервал разницы включает ноль.

**Вывод.** Больше обучающих запусков не добавлялось. Наблюдаемое среднее выше у нового метода, но устойчивое превосходство над ACCEL этими шестью запусками не установлено.

## 14. Проверка сохраненных контрольных точек

**Гипотеза.** Отдельный запуск оценки должен воспроизвести итог обучения и исключить ошибку сохранения или загрузки параметров агента.

**Реализация.** Каждая из 18 контрольных точек шага 30000 загружена через `scripts/evaluate.sh`. На каждом итоговом уровне выполнено 10 попыток. Результат записан в `evaluation.json`.

**Результат.** У всех 18 контрольных точек указан шаг 30000. Средние значения отдельной оценки: DR `0.4417 ± 0.1377`, PLR `0.3625 ± 0.1984`, ACCEL `0.5125 ± 0.2158`, итоговый метод `0.5500 ± 0.1670`.

**По уровням.**

| Метод | SixteenRooms | SixteenRooms2 | Labyrinth | LabyrinthFlipped | Labyrinth2 | StandardMaze | StandardMaze2 | StandardMaze3 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| DR | 0.967 | 0.767 | 0.567 | 0.000 | 0.267 | 0.000 | 0.267 | 0.700 |
| PLR MaxMC | 1.000 | 0.400 | 0.167 | 0.367 | 0.000 | 0.333 | 0.267 | 0.367 |
| ACCEL MaxMC | 0.850 | 0.317 | 0.267 | 0.517 | 0.667 | 0.533 | 0.333 | 0.617 |
| Итоговый метод | 0.867 | 0.617 | 0.833 | 0.700 | 0.400 | 0.183 | 0.200 | 0.600 |

**Наблюдение.** Главный выигрыш нового метода приходится на SixteenRooms2, Labyrinth и LabyrinthFlipped. ACCEL лучше на Labyrinth2 и семействе StandardMaze.

**Вывод.** Контрольные точки загружаются правильно. Полные проверки отдельных частей метода не запускались; для их разбора используются уже проведенные короткие сравнения.
